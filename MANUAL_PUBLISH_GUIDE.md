# 🚀 دليل النشر اليدوي للإصدار 0.1.2

**المشكلة:** GitHub Actions لم ينشر الحزمة تلقائيًا  
**الحل:** النشر اليدوي على npm

---

## ✅ الحالة الحالية

- ✅ الكود على main: ec11728
- ✅ package.json: 0.1.2
- ✅ Git Tag: v0.1.2 (مدفوع)
- ✅ Build: dist/ موجود
- ❌ npm: لا يزال 0.1.1

---

## 📝 خطوات النشر اليدوي

### الخطوة 1: تسجيل الدخول إلى npm

```bash
cd ~/dev/rdapify/RDAPify
npm login
```

**سيطلب منك:**
- Username: (اسم المستخدم على npmjs.com)
- Password: (كلمة المرور)
- Email: (البريد الإلكتروني)
- OTP: (رمز التحقق بخطوتين إذا كان مفعلاً)

### الخطوة 2: التحقق من تسجيل الدخول

```bash
npm whoami
# يجب أن يظهر اسم المستخدم
```

### الخطوة 3: التحقق من الحزمة

```bash
# التحقق من النسخة
grep '"version"' package.json
# يجب أن يظهر: "version": "0.1.2"

# التحقق من البناء
ls -la dist/
# يجب أن يكون المجلد موجود
```

### الخطوة 4: النشر

```bash
npm publish
```

**إذا ظهرت رسالة خطأ "You do not have permission":**
```bash
# تحقق من أنك مالك الحزمة أو لديك صلاحيات
npm owner ls rdapify

# إذا لم تكن في القائمة، اطلب من المالك إضافتك:
# npm owner add <username> rdapify
```

### الخطوة 5: التحقق من النشر

```bash
# انتظر 10-30 ثانية ثم:
npm view rdapify version
# يجب أن يظهر: 0.1.2

# تحقق من جميع النسخ
npm view rdapify versions
# يجب أن يظهر: [..., "0.1.1", "0.1.2"]
```

---

## 🔍 استكشاف الأخطاء

### خطأ: "You must be logged in"
```bash
npm login
```

### خطأ: "You do not have permission"
```bash
# تحقق من المالكين
npm owner ls rdapify

# إذا لم تكن في القائمة، اتصل بالمالك
```

### خطأ: "version already exists"
```bash
# تحقق من النسخة الحالية
npm view rdapify version

# إذا كانت 0.1.2 موجودة، فالنشر تم بالفعل!
```

### خطأ: "prepublishOnly script failed"
```bash
# شغل الاختبارات يدويًا
npm test

# إذا فشلت، أصلح الأخطاء أولاً
```

---

## 🎯 الأوامر الكاملة (نسخ ولصق)

```bash
# 1. الانتقال للمجلد
cd ~/dev/rdapify/RDAPify

# 2. التأكد من الفرع
git branch --show-current
# يجب أن يظهر: main

# 3. التأكد من النسخة
grep '"version"' package.json
# يجب أن يظهر: "version": "0.1.2"

# 4. تسجيل الدخول
npm login

# 5. التحقق من تسجيل الدخول
npm whoami

# 6. النشر
npm publish

# 7. التحقق
npm view rdapify version
```

---

## ⚠️ ملاحظات مهمة

1. **تأكد من تسجيل الدخول** بحساب له صلاحيات على حزمة `rdapify`
2. **لا تنشر من فرع آخر** - يجب أن تكون على `main`
3. **تأكد من البناء** - مجلد `dist/` يجب أن يكون موجود
4. **الاختبارات** - سيتم تشغيلها تلقائيًا قبل النشر (prepublishOnly)

---

## 📊 بعد النشر الناجح

### 1. التحقق من npm
```bash
npm view rdapify version
# النتيجة: 0.1.2 ✅
```

### 2. اختبار التثبيت
```bash
mkdir /tmp/test-rdapify
cd /tmp/test-rdapify
npm init -y
npm install rdapify@0.1.2
node -e "const rdapify = require('rdapify'); console.log('Success!');"
```

### 3. التحقق من الموقع
افتح: https://www.npmjs.com/package/rdapify
- يجب أن ترى: **Version 0.1.2**
- تاريخ النشر: اليوم

### 4. إنشاء GitHub Release (اختياري)
```bash
# يمكنك إنشاؤه يدويًا من:
# https://github.com/rdapify/RDAPify/releases/new

# أو باستخدام GitHub CLI:
gh release create v0.1.2 \
  --title "v0.1.2: Interactive Playground" \
  --notes "See CHANGELOG.md for details"
```

---

## 🎉 الخلاصة

**الخطوات:**
1. `npm login` - تسجيل الدخول
2. `npm whoami` - التحقق
3. `npm publish` - النشر
4. `npm view rdapify version` - التأكد

**الوقت المتوقع:** 1-2 دقيقة

---

**ابدأ الآن بتنفيذ الأوامر أعلاه!** 🚀
