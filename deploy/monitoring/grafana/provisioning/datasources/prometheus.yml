apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    uid: rdapify-prometheus
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      httpMethod: GET
      timeInterval: 15s
    editable: false
