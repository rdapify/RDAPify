/**
 * Library version — single source of truth.
 * Imported by Fetcher (User-Agent), UsageTelemetry, and re-exported from src/index.ts.
 * Update this constant (and package.json) when bumping the package version.
 *
 * @module shared/constants/version
 */

/** Current library version. */
export const VERSION = '0.3.2';
