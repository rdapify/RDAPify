# 🪟 تثبيت Node.js على Windows

**التاريخ**: 22 يناير 2025  
**نظام التشغيل**: Windows  
**الحالة**: Node.js غير مثبت - يجب التثبيت أولاً

---

## ⚠️ المشكلة الحالية

عند محاولة تشغيل `node --version` أو `npm install`، تظهر الرسالة:

```
node : The term 'node' is not recognized as the name of a cmdlet, function, script file, or operable program.
```

**السبب**: Node.js غير مثبت على النظام.

---

## 📥 طريقة التثبيت (موصى بها)

### الخيار 1: التثبيت من الموقع الرسمي (الأسهل)

#### الخطوة 1: تحميل Node.js

1. افتح المتصفح واذهب إلى: **https://nodejs.org/**
2. ستجد زرين:
   - **LTS (Long Term Support)** - موصى به ✅
   - **Current** - أحدث إصدار

3. **اختر LTS** (حالياً Node.js 20.x)

4. سيتم تحميل ملف: `node-v20.x.x-x64.msi`

#### الخطوة 2: تثبيت Node.js

1. افتح الملف المحمل `node-v20.x.x-x64.msi`
2. اتبع معالج التثبيت:
   - اضغط **Next**
   - اقبل الترخيص (I accept)
   - اختر مسار التثبيت (الافتراضي: `C:\Program Files\nodejs\`)
   - **مهم**: تأكد من تحديد "Add to PATH" ✅
   - اضغط **Install**
   - انتظر حتى ينتهي التثبيت (1-2 دقيقة)
   - اضغط **Finish**

#### الخطوة 3: التحقق من التثبيت

1. **أغلق PowerShell الحالي** (مهم جداً!)
2. افتح PowerShell جديد
3. شغل الأوامر التالية:

```powershell
# تحقق من Node.js
node --version

# يجب أن تظهر: v20.x.x

# تحقق من npm
npm --version

# يجب أن تظهر: 10.x.x
```

**إذا ظهرت الأرقام**: تهانينا! التثبيت نجح ✅

---

### الخيار 2: التثبيت باستخدام Chocolatey (للمتقدمين)

إذا كان لديك Chocolatey مثبت:

```powershell
# تثبيت Node.js LTS
choco install nodejs-lts -y

# أو أحدث إصدار
choco install nodejs -y
```

---

### الخيار 3: التثبيت باستخدام Winget (Windows 10/11)

```powershell
# تثبيت Node.js LTS
winget install OpenJS.NodeJS.LTS

# أو أحدث إصدار
winget install OpenJS.NodeJS
```

---

## 🔍 استكشاف الأخطاء

### مشكلة 1: "node is not recognized" بعد التثبيت

**السبب**: PowerShell القديم لا يزال مفتوحاً

**الحل**:

1. أغلق PowerShell تماماً
2. افتح PowerShell جديد
3. جرب `node --version` مرة أخرى

---

### مشكلة 2: Node.js مثبت لكن لا يعمل

**السبب**: PATH غير محدث

**الحل**:

```powershell
# تحقق من PATH
$env:Path

# يجب أن تجد: C:\Program Files\nodejs\
```

**إذا لم تجده**:

1. افتح **System Properties** (Win + Pause)
2. اضغط **Advanced system settings**
3. اضغط **Environment Variables**
4. في **System variables**، اختر **Path**
5. اضغط **Edit**
6. اضغط **New**
7. أضف: `C:\Program Files\nodejs\`
8. اضغط **OK** على جميع النوافذ
9. أغلق وأعد فتح PowerShell

---

### مشكلة 3: خطأ في الصلاحيات أثناء التثبيت

**الحل**:

1. انقر بزر الماوس الأيمن على ملف `.msi`
2. اختر **Run as administrator**
3. أكمل التثبيت

---

## ✅ بعد التثبيت الناجح

### الخطوة 1: التحقق من الإصدارات

```powershell
# Node.js
node --version
# النتيجة المتوقعة: v20.x.x

# npm
npm --version
# النتيجة المتوقعة: 10.x.x

# npx
npx --version
# النتيجة المتوقعة: 10.x.x
```

---

### الخطوة 2: تحديث npm (اختياري)

```powershell
# تحديث npm لأحدث إصدار
npm install -g npm@latest
```

---

### الخطوة 3: العودة لمشروع RDAPify

الآن يمكنك متابعة بناء المشروع:

```powershell
# الانتقال لمجلد المشروع
cd C:\Users\haza\Desktop\RDAPify

# تثبيت dependencies
npm install

# إعداد Husky
npm run prepare

# بناء المشروع
npm run build

# التحقق من الأنواع
npm run typecheck

# فحص الكود
npm run lint

# تشغيل الاختبارات
npm test

# اختبار الأمثلة
node examples/basic/domain_lookup.js
```

---

## 📊 الوقت المتوقع

### التثبيت:

- تحميل Node.js: 1-3 دقائق (حسب سرعة الإنترنت)
- تثبيت Node.js: 1-2 دقيقة
- التحقق: 10 ثواني

**المجموع**: 3-6 دقائق

### بعد التثبيت (بناء RDAPify):

- `npm install`: 2-3 دقائق
- `npm run build`: 10-15 ثانية
- الاختبارات: 5-10 ثواني

**المجموع الكلي**: 10-15 دقيقة

---

## 🎯 Checklist التثبيت

- [ ] تحميل Node.js LTS من nodejs.org
- [ ] تشغيل ملف `.msi` كـ Administrator
- [ ] التأكد من تحديد "Add to PATH"
- [ ] إكمال التثبيت
- [ ] إغلاق PowerShell القديم
- [ ] فتح PowerShell جديد
- [ ] تشغيل `node --version` (يجب أن يعمل)
- [ ] تشغيل `npm --version` (يجب أن يعمل)
- [ ] الانتقال لمجلد RDAPify
- [ ] تشغيل `npm install`

---

## 💡 نصائح مهمة

### 1. استخدم LTS دائماً

- LTS = Long Term Support
- أكثر استقراراً
- موصى به للإنتاج

### 2. أغلق PowerShell بعد التثبيت

- PATH لا يتحدث إلا بعد إعادة فتح Terminal
- هذا سبب شائع للمشاكل

### 3. استخدم Administrator عند الحاجة

- بعض العمليات تحتاج صلاحيات
- انقر بزر الماوس الأيمن → Run as administrator

### 4. تحديث npm

- npm يأتي مع Node.js
- لكن يمكن تحديثه بشكل منفصل
- `npm install -g npm@latest`

---

## 🔗 روابط مفيدة

- **Node.js الرسمي**: https://nodejs.org/
- **npm الرسمي**: https://www.npmjs.com/
- **Node.js على GitHub**: https://github.com/nodejs/node
- **دليل Node.js**: https://nodejs.org/en/docs/

---

## 📞 إذا واجهت مشاكل

### 1. تحقق من المتطلبات

- Windows 10 أو أحدث
- 50 MB مساحة فارغة
- اتصال بالإنترنت

### 2. جرب إعادة التثبيت

```powershell
# إلغاء التثبيت من Control Panel
# ثم إعادة التثبيت من nodejs.org
```

### 3. تحقق من Antivirus

- بعض برامج الحماية تمنع التثبيت
- عطّل Antivirus مؤقتاً أثناء التثبيت

### 4. استخدم Installer بديل

- جرب Chocolatey أو Winget
- أو حمل ZIP version من nodejs.org

---

## 🎉 الخطوة التالية

بعد تثبيت Node.js بنجاح:

1. ✅ Node.js مثبت
2. ✅ npm متوفر
3. ⏭️ **التالي**: تشغيل `npm install` في مشروع RDAPify

**راجع**: `BUILD_PHASE_READY.md` للخطوات التالية

---

**آخر تحديث**: 22 يناير 2025  
**نظام التشغيل**: Windows  
**الحالة**: في انتظار تثبيت Node.js  
**الخطوة التالية**: تحميل وتثبيت Node.js من nodejs.org 🚀
